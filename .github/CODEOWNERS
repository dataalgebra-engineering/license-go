# Maps .ai review roles to ENFORCED approvals (branch ruleset: require_code_owner_review).
# Teams under @dataalgebra-engineering must exist before "require code owner review"
# is flipped on; until then rules are advisory. license-go is the shared WIRE
# CONTRACT, so the contract surface needs security-architect sign-off.

# The contract surface (root *.go: claims, verify, jwks, wire, plan)
/*.go                     @daedmonds

# Consumer + examples
/consumer/**              @daedmonds
/examples/**              @daedmonds

# Docs + CI
/docs/**                  @daedmonds
/.github/**               @daedmonds

# Test code
/*_test.go                @daedmonds
